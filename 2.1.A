#include <stdio.h>
int main(){
    int length, breadth;
    printf("what is the length of rectangle \n",length);
    scanf("%d",&length);
    
    printf("what is the breadth of rectangle \n",breadth);
    scanf("%d",&breadth);

    printf("perimeter OF RACTANGLE IS %d",2*(length+breadth));
    return 0;
}


